// I used this to clean up the data.

/*
d3.text('data/timeline/DemocraticPrimaryDebateSchedule.csv', d3.autotype).then(data => {
    const regex = /\[\d{3}\]/g;

    console.log(data.replace(regex, ""));
});
*/